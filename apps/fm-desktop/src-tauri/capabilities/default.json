{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the main window. Allow-lists only the commands this host actually implements (spec §22): no default full-filesystem plugin access.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
