/**
 * Generated by Orval from frontend/openapi/openapi.json — do not edit by hand.
 * Run `pnpm api:generate` (or `pnpm api:check`) to regenerate.
 */
import type { LocationDto } from './locationDto';
import type { SystemLocationKindDto } from './systemLocationKindDto';

/**
 * A navigable location discovered by the host operating system.
 */
export interface SystemLocationDto {
  /** Classification used to group locations in the frontend. */
  kind: SystemLocationKindDto;
  /** Existing provider-neutral local location. */
  location: LocationDto;
  /** Display label supplied by the OS folder. */
  name: string;
  /**
     * Optional advisory provider name; navigation never depends on it.
     * @nullable
     */
  providerHint?: string | null;
}
