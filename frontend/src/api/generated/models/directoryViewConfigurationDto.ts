/**
 * Generated by Orval from frontend/openapi/openapi.json — do not edit by hand.
 * Run `pnpm api:generate` (or `pnpm api:check`) to regenerate.
 */
import type { ColumnConfigurationDto } from './columnConfigurationDto.ts';
import type { PersistedFilterDto } from './persistedFilterDto.ts';
import type { SortDescriptorDto } from './sortDescriptorDto.ts';

/**
 * Persisted view configuration for a directory listing: sorting, columns
 * and filters. Contains no frontend-only selection/cursor fields.
 */
export interface DirectoryViewConfigurationDto {
  /** Per-column width and visibility. */
  columns: ColumnConfigurationDto[];
  /** Whether directories are grouped before files. */
  foldersFirst: boolean;
  quickFilter?: null | PersistedFilterDto;
  /** Whether hidden entries are shown. */
  showHidden: boolean;
  /** The active sort descriptors, in priority order. */
  sort: SortDescriptorDto[];
}
