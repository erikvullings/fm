/**
 * Generated by Orval from frontend/openapi/openapi.json — do not edit by hand.
 * Run `pnpm api:generate` (or `pnpm api:check`) to regenerate.
 */
import type { DirectoryViewPatchDto } from './directoryViewPatchDto';
import type { LocationDto } from './locationDto';
import type { NavigationModeDto } from './navigationModeDto';
import type { WorkspaceLayoutDto } from './workspaceLayoutDto';

/**
 * A semantic workspace mutation (spec §5.3.9). Every variant carries the
 * `workspaceId` it targets and the `expectedRevision` it was issued
 * against, so the service can detect a stale-view conflict (spec §5.3.10)
 * before applying the mutation.
 */
export type WorkspaceCommandDto = {
  /**
     * The revision this command was issued against.
     * @minimum 0
     */
  expectedRevision: number;
  /** The new, non-empty name. */
  name: string;
  type: 'renameWorkspace';
  /** The target workspace. */
  workspaceId: string;
} | {
  /**
     * The revision this command was issued against.
     * @minimum 0
     */
  expectedRevision: number;
  /** The pane to activate. */
  paneId: string;
  type: 'setActivePane';
  /** The target workspace. */
  workspaceId: string;
} | {
  /**
     * The revision this command was issued against.
     * @minimum 0
     */
  expectedRevision: number;
  /** The new tab's initial location. */
  location: LocationDto;
  /** The pane to append the tab to. */
  paneId: string;
  type: 'addTab';
  /** The target workspace. */
  workspaceId: string;
} | {
  /**
     * The revision this command was issued against.
     * @minimum 0
     */
  expectedRevision: number;
  /** The pane the tab belongs to. */
  paneId: string;
  /** The tab to close. */
  tabId: string;
  type: 'closeTab';
  /** The target workspace. */
  workspaceId: string;
} | {
  /**
     * The revision this command was issued against.
     * @minimum 0
     */
  expectedRevision: number;
  /** The pane the tab belongs to. */
  paneId: string;
  /** The tab to activate. */
  tabId: string;
  type: 'activateTab';
  /** The target workspace. */
  workspaceId: string;
} | {
  /**
     * The revision this command was issued against.
     * @minimum 0
     */
  expectedRevision: number;
  location?: null | LocationDto;
  /** How the navigation affects history. */
  navigationMode: NavigationModeDto;
  /** The pane the tab belongs to. */
  paneId: string;
  /** The tab to navigate. */
  tabId: string;
  type: 'navigateTab';
  /** The target workspace. */
  workspaceId: string;
} | {
  /**
     * The revision this command was issued against.
     * @minimum 0
     */
  expectedRevision: number;
  /** The pane the tab belongs to. */
  paneId: string;
  /** The fields to change. */
  patch: DirectoryViewPatchDto;
  /** The tab to patch. */
  tabId: string;
  type: 'updateView';
  /** The target workspace. */
  workspaceId: string;
} | {
  /**
     * The revision this command was issued against.
     * @minimum 0
     */
  expectedRevision: number;
  /** The new layout; must reference every existing pane exactly once. */
  layout: WorkspaceLayoutDto;
  type: 'updateLayout';
  /** The target workspace. */
  workspaceId: string;
};
